import * as packageInfo from 'package.json';

export const userAgent = `HumbleBundle-Ebook-Downloader/${packageInfo.version}`;
export const SUPPORTED_FORMATS = ['cbz', 'epub', 'pdf_hd', 'pdf', 'mobi'];
